command:
  - uvicorn
  - app:app
  - --host
  - "0.0.0.0"
  - --port
  - "8000"

env:
  - name: DATABRICKS_APP_PORT
    value: "8000"
  - name: COINGECKO_API_KEY
    value: "{{secrets/coingecko/api_key}}"
